<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="META-INF.mybatis.NoticeMapper">

	<!-- Select result_Object -->
	<resultMap type="notice" id="noticeResult">
		<id property="seq" column="seq" />
		<result property="title" column="title" />
		<result property="writer" column="writer" />
		<result property="content" column="content" />
		<result property="regDate" column="regDate" />
		<result property="count" column="count" />
		<result property="likeIt" column="likeIt" />
	</resultMap>

	<select id="noticeDetail" resultType="notice">
		<![CDATA[SELECT*FROM notice WHERE seq=#{seq}]]>
	</select>
	<select id="getNoticeList" parameterType="notice" resultMap="noticeResult">
		<![CDATA[SELECT*FROM notice ]]>
	</select>

</mapper>